datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Loan {
  id              Int   @id @default(autoincrement())
  value           Float
  monthlyInterest Float
  monthlyValue    Float
  monthCount      Int
  totalInterest   Float
}
